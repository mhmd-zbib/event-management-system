server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/Development/Projects/Springboot/propal-config
          clone-on-start: false
          default-label: master
